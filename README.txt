#structure:

phonegap project is in the root directory
android project is in the android-project folder

#dependency

 - npm/yarn
 - phonegap

#build

> npm install | yarn
> [phonegap ]cordova build browser #or just build, but this is faster
> phonegap serve
